import { StyleSheet } from 'react-native'

const styles = StyleSheet.create({
  wrapper: {
    paddingTop: 10,
    paddingHorizontal: 10
  },
  audienceLevel: {
    padding: 10,
    paddingBottom: 30,
    borderRadius: 12,
    borderColor: '#DADADA',
    borderWidth: 1,
    backgroundColor: '#FFF'
  },
  paddingTop15: {
    paddingTop: 15
  },
  marginTop20: { marginTop: 20 },
  durations: { justifyContent: 'flex-start' },
  levelPercentage: { justifyContent: 'space-between' }
})

export default styles
