import { StyleSheet } from 'react-native'

const styles = StyleSheet.create({
  wrapper: {
    marginVertical: 8,
    backgroundColor: '#fff'
  },
  slotIcon: { width: '100%', height: '100%' }
})

export default styles
